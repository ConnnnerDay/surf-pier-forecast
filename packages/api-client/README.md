# API client workspace

This package is reserved for the generated TypeScript client sourced from the
FastAPI OpenAPI document. It intentionally contains no hand-written forecast
types. Client generation is introduced with the versioned API contract sprint.
