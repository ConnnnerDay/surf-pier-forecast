# Local Next.js and BFF configuration. These values are not production secrets.
APP_ENV=development
INTERNAL_API_BASE_URL=http://127.0.0.1:8000
DATABASE_URL=postgresql://surf_pier:local-development-only@127.0.0.1:5432/surf_pier

# Placeholder values for the authentication and signed-BFF sprints. Replace
# them with independently generated secrets outside local development.
AUTH_SECRET=local-auth-secret-replace-before-deploy
INTERNAL_API_ACTIVE_KEY_ID=local-v1
INTERNAL_API_ACTIVE_SECRET=local-internal-secret-replace-before-deploy
