# Runtime dependencies — pinned to compatible minor versions.
# Run `pip install -r requirements.txt` to reproduce the exact environment.
# Regenerate with: pip-compile requirements.in  (or uv pip compile)
Flask>=3.1,<4.0
Werkzeug>=3.1,<4.0
requests>=2.32,<3.0
beautifulsoup4>=4.12,<5.0

# Production WSGI server (recommended outside development)
gunicorn>=21.2,<26.0

# WebAuthn / passkey support (Face ID, Touch ID, Windows Hello)
webauthn>=2.7,<3.0

# Cryptography — used for Apple Sign In JWT client_secret generation (ES256).
# Also a transitive dependency of webauthn; pinned here explicitly.
cryptography>=41.0,<45.0

# ── Geospatial / GIS extras (all optional — app degrades gracefully without them) ──
#
# geopandas: spatial clip + shapefile loading in services/natural_earth.py.
#   Requires: GDAL, GEOS, Proj system libraries.
#   Install system deps first:
#     Debian/Ubuntu: sudo apt-get install libgdal-dev libgeos-dev libproj-dev
#     macOS (Homebrew): brew install gdal geos proj
#   Then: pip install geopandas>=0.14,<2.0
#
# pandas: DataFrame utilities used by datagov.py aggregation helpers.
#   pip install pandas>=2.0,<3.0
#
# Uncomment these lines to enable full GIS functionality:
# geopandas>=0.14,<2.0
# pandas>=2.0,<3.0
